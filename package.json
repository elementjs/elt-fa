{
  "name": "elt-fa",
  "version": "0.2.1",
  "repository": "https://github.com/domicjs/elt-fa",
  "description": "some description",
  "license": "MIT",
  "main": "./index.js",
  "typings": "./index.d.ts",
  "scripts": {
    "build:js": "scripts/mkicons.js && mkdir -p build && tsc && mv build/* .",
    "build": "npm run build:js",
    "install": "npm run build",
    "clean": "rm *.js *.js.map *.d.ts ; mv src/index.tsx . ; rm src/*.tsx ; mv index.tsx src/",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@fortawesome/fontawesome-pro": "^5.10.1",
    "osun": "0.2.0"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.10.1"
  }
}
