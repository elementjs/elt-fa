{
  "name": "elt-fa",
  "version": "0.1.1",
  "repository": "https://github.com/domicjs/elt-fa",
  "description": "some description",
  "license": "MIT",
  "main": "./index.js",
  "typings": "./index.d.ts",
  "scripts": {
    "build:js": "./mkicons.js && mkdir -p build && tsc && mv build/* .",
    "build": "npm run build:js",
    "prepublish": "npm run build",
    "watch": "tsc -w"
  },
  "dependencies": {
    "osun": "0.2.0"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.7.1"
  }
}
